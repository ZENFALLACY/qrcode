# Leave empty for local dev (Vite proxies /api → http://localhost:5000).
# In production (e.g. Vercel), set to your backend URL, no trailing slash:
# VITE_API_URL=https://your-api.onrender.com

VITE_API_URL=
